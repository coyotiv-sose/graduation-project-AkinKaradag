services:
  api:
    image: karalog-api
    build:
      context: backend
      dockerfile: Dockerfile
    depends_on:
      - mongodb
    ports:
      - '3000:3000'
    volumes:
      - ./backend/src:/app/src
    env_file:
      - backend/.env
    environment:
      - MONGODB_CONNECTION_STRING=mongodb://mongodb:27017/karalog

  mongodb:
    image: mongo
    ports:
      - '27017:27017'
    volumes:
      - ./data:/data/db
